# Engine objects that reference a symbol only a shell object defines.
#
# EMPTY. B7's seams closed the last six: the option snapshot (searchoptions.h)
# replaced OptionsMap in search, timeman, the thread pool and the Syzygy prober,
# and the two UCI renderers moved into the engine beside the domain knowledge
# they already used.
#
# A new entry here is a new edge. There is no debt left to grandfather, so the
# next one to appear is the first.
